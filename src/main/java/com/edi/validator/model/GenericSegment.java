package com.edi.validator.model;

public class GenericSegment extends EDISegment {
    public GenericSegment(String segmentCode, String line, int lineNumber) {
        super(segmentCode, line, lineNumber);
    }
} 